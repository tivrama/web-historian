- Will serve pages over the web, using RESTful API
  - accept URLs
  - use POST request to store submitted URLs to single file

- Be able to read the stored URL list and fetch the pages, then store the HTML
      - an Object
            - key = URL
            - value = HTML

      - If user requests a URL
      - Check if it is in our Obj already
      - If not, We will run the URL through retrieval and storage method

      - Set all of this up on Cron
      http://benr75.com/pages/using_crontab_mac_os_x_unix_linux

      